server:
  port: 8049
spring:
  application:
    name: database-migration-platform

migration:
  platform:
    config-location: classpath:database-migration-platform.yml
    audit-directory: build/audit
    allow-production-rebuild: false
    default-requested-by: dba-operator

logging:
  level:
    org.flywaydb: DEBUG
