server:
  port: 8049
spring:
  application:
    name: migration-platform

migration:
  platform:
    config-location: classpath:migration-platform.yml
    audit-directory: build/audit
    allow-production-rebuild: false
    default-requested-by: dba-operator

logging:
  level:
    org.flywaydb: DEBUG
